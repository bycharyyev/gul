VITE_API_URL="http://localhost:4000/api"
VITE_NETDATA_URL="http://localhost:19999"

# Optional — error tracking (Sentry). Unset means lib/sentry.ts never calls Sentry.init and
# nothing is sent anywhere.
VITE_SENTRY_DSN=""
