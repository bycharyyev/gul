{
  "name": "@topup-hub/agent-orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no eslint config for this package yet)\"",
    "test": "node --import tsx --test src/**/*.test.ts",
    "task": "tsx src/cli.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@topup-hub/config": "workspace:*",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
